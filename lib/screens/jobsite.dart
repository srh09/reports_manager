import 'package:flutter/material.dart';

class JobsiteScreen extends StatelessWidget {
  static const routeName = '/jobsite';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
